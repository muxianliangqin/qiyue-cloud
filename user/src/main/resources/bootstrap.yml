spring:
  application:
    name: qiyue-cloud-user
  cloud:
    config:
      profile: prod
      uri: http://localhost:7040/
      label: master

management:
  endpoints:
    web:
      exposure:
        include: refresh
